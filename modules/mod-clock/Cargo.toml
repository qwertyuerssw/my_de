[package]
name = "mod-clock"
version = "0.1.0"
edition = "2021"

[dependencies]
# Наш IPC-протокол
de-ipc = { path = "../../de-ipc" }

# Сериализация
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Работа со временем
chrono = "0.4"