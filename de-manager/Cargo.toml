[package]
name = "de-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
# Нам обязательно нужен доступ к нашему общему протоколу IPC:
de-ipc = { path = "../de-ipc" }

# И библиотека для парсинга JSON:
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"